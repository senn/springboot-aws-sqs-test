# Spring Boot AWS SQS Test App

A small example app to produce and consume messages/events in an AWS SQS queue.

Check the commits to see gradual changes:

1. old AWS SDK
2. Spring Cloud AWS SDK
3. some improvements

## Remarks

Locked to Spring Boot 3 because the Spring Cloud AWS SDK to support Spring Boot 4
is not publicly available at time of writing.

